# Expense-Tracker
To run this, you need to install the Flask depency. Make sure to install it before running it, then run app.py and go to the address it gives you in the terminal. It's a really simple expense tracker for a personal noob project.
