# Expense-Tracker
To run this, you need to install the Flask depency. You can just type pip install -r requirements.txt before running it. Afterwards, you can run the app.py and go to the address it gives you in the terminal!
